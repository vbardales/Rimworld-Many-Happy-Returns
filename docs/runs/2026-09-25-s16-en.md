# Existing-save pass, English, 2026-09-25 (S16, log audited by LoadAudit)

Two runs of the same scenario, the first red for a real reason and the second green.

| Request | Label SHA | exitReason | Result |
| --- | --- | --- | --- |
| `20260925-204814-487-bb3d` | `4978cb1` | `failed` | 0 of 1: the scenario played through, the audit at its end found one warning |
| `20260925-221001-441-9a0b` | `ccf62c4` | `passed` | 1 of 1, `load-audit`: 520 log lines read, 14 types, 6 defs, 0 finding, 0 translation problem |

- **Command**: `Submit-PickleRun.ps1 -Mod ManyHappyReturns -Language English -DepMap wsl-deps.avec-loadaudit.map -Filter 16-existing-save-regression` (evidence `2026-09-25-s16-en`, `2026-09-25-s16-rejeu-en`)
- **Set name**: `avec-loadaudit`
- **Suite state of the replay**: `ccf62c4`; nothing under `Mod/` or `Tests/` changed between it and the head after the run (`git diff --stat ccf62c4..HEAD -- Mod Tests`: empty)
- **Scenario**: `a birthday played through on a saved colony, then the settings opened and closed`: letter, wish, verdict on the fixture save, the settings page opened and closed, then Nelim's Pickle Tools' `the load of the mod "nelim.manyhappyreturns" is clean`
- **Evidence**: on disk, ignored by git: `junit.xml`, `summary.json`, `summary.md`, `Player.log` of both runs. The report copies and the captures were deleted.

## The red run

The scenario itself passed. The audit at its end failed on a warning the game logs at every start: `Mod Many Happy Returns - Pickle tests dependency
(nelim.manyhappyreturns) needs to have <downloadUrl> and/or <steamWorkshopUrl> specified`. It is a real defect of the test companion's `About.xml`
(Pickle's own dependency already had its URL); it was in every log since the first run and no check had read it. The audit attributed it to the mod because the
message names the mod's assembly. Fixed at the source (`steamWorkshopUrl` for item 3806762201), not with an exception in the audit.

## What the green run shows, and does not

The audit read the mod as a mod with code: 14 types and 6 defs, so LoadAudit's reading of assemblies and defs works on a real mod, which its own author
had not been able to show (his companion has none). The log lines that remain are the game's and the fixture's: Steamworks not initialised, the saved
map being from 1.6.4633 while the game is 1.6.4871, hidden ritual precepts added on load, and the four `did not load any content` lines of the companions.
Nothing from the mod. The step does not prove a key that is never displayed, a def never loaded, or a path never executed.
The French pass is the other half (the key check reads the active language's data); it is requested separately.
