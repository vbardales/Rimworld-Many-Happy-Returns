# Existing-save pass, French, 2026-09-25 (S16, log audited by LoadAudit)

| Request | Label SHA | exitReason | Result |
| --- | --- | --- | --- |
| `20260925-234223-871-66b3` | `652d5ee` | `passed` | 1 of 1, 1 attempt, 12.4 s, `load-audit`: 520 log lines read, 14 types, 6 defs, 0 finding, 0 translation problem |

- **Command**: `Submit-PickleRun.ps1 -Mod ManyHappyReturns -Language French -DepMap wsl-deps.avec-loadaudit.map -Filter 16-existing-save-regression` (evidence `2026-09-25-s16-fr`)
- **Suite state**: nothing under `Mod/` or `Tests/` changed between `652d5ee` and the head after the run.
- **What it adds to the English run**: the key check reads the active language's data, so "0 translation problem" is now shown for French too.
- **Evidence**: `junit.xml`, `summary.json`, `summary.md`, `Player.log` on disk, ignored by git; `report.html` and `messages.ndjson` deleted.
